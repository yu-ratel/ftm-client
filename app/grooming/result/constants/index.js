const image_path = "/grooming-test/images";

const LEVEL_IMG = `${image_path}/result.png`;
const LANDING_BUTTON_IMG = `${image_path}/landing_button.png`;

const GRADE_CATEGORY = {
  beauty: "스킨케어",
  hygiene: "위생 관리",
  hair: "헤어 스타일링",
  workout: "건강 관리",
  fashion: "패션",
};

const GRADE_MAX_SCORE = 3;

const ROUTE_CARD_TEXT = {
  default: `핏더맨 픽 아이템 \n 추천 받으러 가기`,
  signIn: `핏더맨에 합류하고 \n 아이템 추천 받으러 가기`,
};

export {
  LEVEL_IMG,
  LANDING_BUTTON_IMG,
  GRADE_CATEGORY,
  GRADE_MAX_SCORE,
  ROUTE_CARD_TEXT,
};
